# Copilot Rules для проекта RNGP_MOSCOW_2025.11.17

## Структура проекта

```
RNGP_MOSCOW_2025.11.17/
├── parking.html              # Главная страница калькулятора
├── parkingData.js            # База данных (районы, коэффициенты, параметры)
├── parkingEngine.js          # Логика инициализации, поиска адреса, селекторов
├── shared.js                 # Общие утилиты (если потребуются)
├── style.css                 # Стили (если используются, иначе Pico CSS из CDN)
└── COPILOT_RULES.md          # Этот файл
```

## Соглашения об именовании

### JavaScript

- **Переменные:** camelCase (например, `parkingData`, `districtSelect`, `ttkPosSelect`)
- **Функции:** camelCase (например, `initApp()`, `matchDistrict()`, `handleAddressSearch()`)
- **Константы:** UPPER_SNAKE_CASE (например, `TIMEOUT_WAIT_PARKINGDATA = 5000`)
- **DOM элементы:** переменные заканчиваются на `-Select` или `-Input` (например, `districtSelect`, `objAddress`)
- **Приватные функции:** префикс `_` или определять внутри другой функции (например, `_matchDistrict()`)

### CSS & HTML

- **CSS классы:** kebab-case (например, `.yandex-card`, `.full-address-row`)
- **ID элементов:** camelCase (например, `id="objAddress"`, `id="districtSelect"`)
- **Data-атрибуты:** data-kebab-case (например, `data-district-code`)

## Архитектура и соглашения

### parkingData.js

- Экспортирует объект `parkingData` с ключами:
  - `districts` — словарь {название: [K2_внутри_ТТК, K2_снаружи_ТТК, Зона_К3]}
  - `ttk_positions` — массив [{value, label}, ...]
  - `k3_ratios` — словарь коэффициентов урбанизации
  - `infrastructure` — объект с массивами (metro, bus_stops, bus_intervals)
  - `living_types` — массив типов жилья с весами
  - `x2_rates` — словарь нормативов по назначению
  - `ev_share` — словарь доли электромобилей по годам
- В конце файла обязательно: `window.parkingData = parkingData;`
- **Все тексты/метки берутся из этого объекта** — никаких хардкодов в parkingEngine.js

### parkingEngine.js

- **Функция `waitForParkingData(timeout)`** — Promise-обёртка для ожидания загрузки parkingData
- **Функция `initApp()`** — async, главная инициализация (селекторы, обработчики)
- **Функция `initDistrictSelect(select)`** — заполняет селектор районов из parkingData.districts
- **Функция `handleAddressSearch(rawAddress)`** — async, поиск адреса через Nominatim
- **Функция `matchDistrict(addressObj, fullName)`** — сопоставление найденного адреса с районом
- **Функция `showYandexLink(address)`** — показ ссылки на Яндекс Карты
- **Функция `clearSearchResults()`** — очистка результатов поиска
- **Функция `calculate()`** — заглушка для триггера пересчёта (переопределяется при необходимости)

## Правила кода

### Общие

1. **Нет хардкодов в коде** — все текстовые значения из parkingData.js
2. **Проверка наличия элементов DOM:** всегда перед использованием
   ```javascript
   const el = document.getElementById('id');
   if (!el) {
       console.error("Элемент не найден!");
       return;
   }
   ```
3. **Логирование:** используй console.log/warn/error с префиксами:
   - `✅` — успех
   - `❌` — ошибка
   - `⚠` — предупреждение
   - `📊` — информация о расчётах
   - `🔍` — поиск/отладка
   - `🔧` — инициализация

4. **Асинхронные операции:** явно указывай `async`/`await`, обрабатывай ошибки
5. **Timeout/Debounce:** используй переменные для управления (например, `searchTimeout`)

### parkingEngine.js: Доступ к parkingData

**Правильно:**
```javascript
const PD = (typeof parkingData !== 'undefined') ? parkingData : (window?.parkingData || null);
if (PD && PD.districts) {
    // работаем с PD.districts
}
```

**Или упрощённо** (если parkingData уже в window):
```javascript
if (window.parkingData && window.parkingData.districts) {
    // работаем с window.parkingData
}
```

## Функциональные требования

### Селекторы

- **districtSelect** — заполняется из parkingData.districts (ключи, отсортированы)
- **ttkPos** — заполняется из parkingData.ttk_positions
- **metroAccess** — заполняется из parkingData.infrastructure.metro
- **rnsYear** — заполняется из parkingData.ev_share (ключи)

Каждый селектор должен иметь:
- Placeholder-опцию (disabled, selected по умолчанию)
- Event listener на `change`, вызывающий `calculate()`

### Поиск адреса

- API: Nominatim (OpenStreetMap)
- URL: `https://nominatim.openstreetmap.org/search?q=...`
- Параметры: `format=json`, `addressdetails=1`, `accept-language=ru`, `limit=1`
- Результат: полный адрес + автоматический поиск района

### Визуальная обратная связь

- Поиск адреса: `input.style.borderColor = "#10adff"` (синий — ищется)
- Успех: `select.style.borderColor = "#28a745"` (зелёный) на 2 сек
- Ошибка: `input.style.borderColor = "#dc3545"` (красный) на 2 сек
- Неопределено: `select.style.borderColor = "#ffc107"` (жёлтый) на 2 сек

## Порядок загрузки (в parking.html)

```html
<script src="parkingData.js" defer></script>
<script src="parkingEngine.js" defer></script>
<script>
    // Инлайн скрипты (функция calculate и т.д.)
</script>
```

**Не допускать:**
- Динамической загрузки через `document.createElement('script')`
- Условной логики для ожидания (используй `defer`)

## Комментарии и документация

- Каждая функция должна иметь JSDoc-комментарий
  ```javascript
  /**
   * Поиск адреса через Nominatim и автоматический выбор района
   * @param {string} rawAddress - адрес (например, "ул. Маршала Катукова, 24")
   */
  async function handleAddressSearch(rawAddress) { ... }
  ```
- Логичные разделы отмечаются комментариями вида: `// === РАЗДЕЛ ===`
- Трудные места (логика сопоставления района) снабжаются объясняющими комментариями

## Тестирование

- Открыть [parking.html](rngp_2025_11_17/parking.html) в браузере
- Проверить консоль на предмет ошибок
- Убедиться, что селекторы заполняются из parkingData
- При поиске адреса проверить логи и установку района

## Дополнения (добавлять по мере необходимости)

- [ ] Добавить валидацию входных данных (например, проверка площади участка)
- [ ] Реализовать функцию `calculate()` для расчёта машиномест
- [ ] Добавить сохранение/восстановление данных (localStorage)
- [ ] Реализовать экспорт результатов в PDF/Excel
- отладочные сообщения должны не повторяться (об ошибках)
- ответы в chat только на русском нужно направлять
- дополнять этот файл из контеста
- всегда проверять этот файл при каждом чате

- желательно создать доп.инспектора-AI, который будет хранить данные в гитхаб (в разумном объеме данных), либо как правила; при этом будет проверять достаточно данных для работы основого AI-агента, если внутренние коэффиценты сильно низкие, разброс ответов очень большой, вероятность низкая для точной валидации, то лучше уточнять у пользователя (но не бесконечно, не более 2х вопросов), далее уже создаём, но юзеру пишем доп.коммент, что вопрос содержал мало ИД, могут быть расплывчатые ответы, по возможности нужно копить контект в рамках проекта


